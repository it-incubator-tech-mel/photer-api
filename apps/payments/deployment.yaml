apiVersion: apps/v1
kind: Deployment
metadata:
  name: DEPLOYMENT_NAME
  namespace: NAMESPACE
  labels:
    app: PROJECT
spec:
  selector:
    matchLabels:
      project: PROJECT
  template:
    metadata:
      namespace: NAMESPACE
      labels:
        project: PROJECT
    spec:
      containers:
         - name: PROJECT
           image: REGISTRY_HOSTNAME/PROJECT:TAG_VERSION
           ports:
             - containerPort: PORT_CONTAINER

           env:
             - name: PAYMENTS_TCP_PORT
               valueFrom:
                 secretKeyRef:
                   name: payments-micro-production-config-secret
                   key: PAYMENTS_TCP_PORT
             - name: STRIPE_SECRET_KEY
               valueFrom:
                 secretKeyRef:
                   name: payments-micro-production-config-secret
                   key: STRIPE_SECRET_KEY
             - name: STRIPE_WEBHOOK_SECRET
               valueFrom:
                 secretKeyRef:
                   name: payments-micro-production-config-secret
                   key: STRIPE_WEBHOOK_SECRET
             - name: PAYMENTS_DB_URL
               valueFrom:
                 secretKeyRef:
                   name: payments-micro-production-config-secret
                   key: PAYMENTS_DB_URL
             - name: RABBITMQ_URL
               valueFrom:
                 secretKeyRef:
                   name: payments-micro-production-config-secret
                   key: RABBITMQ_URL
             - name: PAYMENTS_TCP_HOST
               valueFrom:
                 secretKeyRef:
                   name: payments-micro-production-config-secret
                   key: PAYMENTS_TCP_HOST